# GCP Labs - Environment Variable Template
# Copy this file to .env and fill in your actual values
# WARNING: Never commit .env to git (it's in .gitignore)

# ============================================
# GCP Project Configuration
# ============================================
# Your GCP project ID (not project name)
# Find it in Cloud Console or run: gcloud config get-value project
GCP_PROJECT_ID=your-project-id-here

# GCP region for deployments
# Common values: us-central1, us-east1, europe-west1, asia-east1
GCP_REGION=us-central1

# ============================================
# Lab 02 - Cloud Run Basic Configuration
# ============================================
# Application name shown in API responses
APP_NAME=GCP Learning Labs

# Custom message returned by the / endpoint
MESSAGE=Hello from Cloud Run

# Spring Boot profile (dev, prod, etc.)
SPRING_PROFILES_ACTIVE=prod

# ============================================
# Lab 05 - Secrets & IAM
# ============================================
# Secret ID in Secret Manager
SECRET_ID=lab02_api_key

# Service account name (not full email)
SA_NAME=sa-lab02-spring

# Cloud Run service name
SERVICE_NAME=lab02-spring

# ============================================
# Lab 06 - Cloud Storage (GCS)
# ============================================
# GCS bucket name (must be globally unique)
# Use format: your-project-id-bucket-name
BUCKET_NAME=your-unique-bucket-name

# ============================================
# Lab 07 - Pub/Sub
# ============================================
# Pub/Sub topic name
PUBSUB_TOPIC=lab07-topic

# Pub/Sub subscription name
PUBSUB_SUBSCRIPTION=lab07-subscription

# ============================================
# Lab 08 - CI/CD (GitHub Actions)
# ============================================
# Workload Identity Provider (format: projects/PROJECT_NUMBER/locations/global/workloadIdentityPools/POOL_ID/providers/PROVIDER_ID)
WIF_PROVIDER=projects/YOUR_PROJECT_NUMBER/locations/global/workloadIdentityPools/github-pool/providers/github-provider

# Workload Identity Service Account email
WIF_SERVICE_ACCOUNT=github-actions@YOUR_PROJECT_ID.iam.gserviceaccount.com

# ============================================
# Usage Instructions
# ============================================
# 1. Copy this file: cp .env.example .env
# 2. Edit .env with your actual values
# 3. Source it when needed: source .env
# 4. Or use with tools that read .env automatically
#
# Security Notes:
# - Never commit .env to git
# - Never hardcode these values in source code
# - Use Secret Manager for sensitive values
# - Share this .env.example file (without actual values)
